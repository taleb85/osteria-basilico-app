/**
 * Chiave localStorage per “resta collegato”: JSON `{ userId: string }`.
 * Usare solo questa costante per evitare disallineamenti tra login, logout e invalidazione sessione.
 */
export const APP_SESSION_STORAGE_KEY = 'app_session';
